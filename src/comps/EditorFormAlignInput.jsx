import styled from "styled-components";

const EditorFormAlignInput = styled.div`display: flex; gap: 0.5em; align-items: center; &>button{ display: flex; align-items: center; justify-content: center; padding: 0.5em; }`;
export default EditorFormAlignInput;