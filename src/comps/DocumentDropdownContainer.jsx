import styled from "styled-components";

const DropdownContainer = styled.div`padding: 0.5em; position: absolute; top: 0; left: 0; display: flex; flex-direction: column; align-items: flex-start; justify-content: center; gap: 0.25em;`;
export default DropdownContainer